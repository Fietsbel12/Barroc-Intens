<Page
    x:Class="BarrocIntens.View.MaintenanceHomepage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.View"
    xmlns:data="using:BarrocIntens.Data"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#FFFDFDFD">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0" Background="#FF1A1A1A" Padding="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="RolTextBlock"
                       Grid.Column="0"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="#FFFFD300"
                       Text="Rol wordt geladen..." />

            <Button x:Name="backButton"
                    Grid.Column="1"
                    Content="Terug"
                    Click="backButton_Click"
                    Padding="12,6"
                    Background="#FFFFD300"
                    Foreground="#22201D"
                    CornerRadius="4"
                    FontWeight="Bold"
                    BorderThickness="0"/>
        </Grid>

        <!-- CONTENT: ListView met uitklapbare taken -->
        <ScrollViewer Grid.Row="1" Margin="10">
            <StackPanel>
                <TextBlock Text="Onderhoudstaken" FontSize="30" FontWeight="Bold" Margin="0,0,0,10"/>

                <ListView x:Name="MaintenanceTaskListView"
                          ItemsSource="{x:Bind TakenLijst, Mode=OneWay}"
                          IsItemClickEnabled="False"
                          SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="data:Taken">
                            <muxc:Expander Margin="0,0,0,10" Background="#FFF5F5F5" CornerRadius="6">
                                <!-- Header met Naam, Tijd en Medewerker -->
                                <muxc:Expander.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{x:Bind TijdString}" FontStyle="Italic" Foreground="Gray"/>
                                        <TextBlock Text="{x:Bind Medewerker.Naam, Mode=OneWay, FallbackValue='Onbekend'}" Foreground="#FF1A1A1A"/>
                                    </StackPanel>
                                </muxc:Expander.Header>

                                <!-- Content: alleen Description -->
                                <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" Padding="15"/>
                            </muxc:Expander>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
