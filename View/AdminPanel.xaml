<Page
    x:Class="BarrocIntens.View.AdminPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#FFFDFDFD">
        <!-- Grid definitions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0"
              Background="#FF1A1A1A"
              Padding="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Rol text -->
            <TextBlock x:Name="RolTextBlock"
                       Grid.Column="0"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="#FFFFD300"
                       Text="Rol wordt geladen..." />

            <!-- Header buttons -->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        Spacing="12"
                        VerticalAlignment="Center">

                <Button x:Name="AdminCreateButton"
                        Content="Create"
                        Click="AdminCreateButton_Click"
                        Background="#FFFFD300"
                        Foreground="#22201D"
                        Padding="12,6"
                        FontWeight="Bold"
                        CornerRadius="4"
                        BorderThickness="0"/>

                <Button x:Name="backButton"
                        Content="Terug"
                        Click="backButton_Click"
                        Padding="12,6"
                        Background="#FFFFD300"
                        Foreground="#22201D"
                        CornerRadius="4"
                        FontWeight="Bold"
                        BorderThickness="0"/>
            </StackPanel>
        </Grid>

        <!-- Content: list of medewerkers -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Width="600"
                        HorizontalAlignment="Center"
                        Padding="20"
                        Spacing="14">

                <TextBlock Text="Alle medewerkers"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#22201D"
                           Margin="0,10,0,20"/>

                <ListView x:Name="MedewerkersListView"
                          Background="#FFF9F9F9"
                          BorderBrush="#FFEFEFEF"
                          BorderThickness="1"
                          CornerRadius="10"
                          Padding="10">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White"
                                    BorderThickness="0,0,0,1"
                                    BorderBrush="#EEE"
                                    Padding="10">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Naam}" FontWeight="Bold" FontSize="18"/>
                                    <TextBlock Text="{Binding MedewerkerRol}" FontSize="14" Foreground="Gray"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
