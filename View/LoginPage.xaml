<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.View.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Padding="30" Grid.Row="1">
            <StackPanel Spacing="15" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">

                <TextBlock Text="Inloggen" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <TextBox x:Name="NaamTextBox" Header="Naam" PlaceholderText="Voer je naam in" />
                <PasswordBox x:Name="WachtwoordBox" Header="Wachtwoord" PlaceholderText="Voer je wachtwoord in" />

                <Button Content="Inloggen" Click="Login_Click" HorizontalAlignment="Stretch" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2" HorizontalAlignment="Right">
            <Button x:Name="backButton"
Margin="20,0,0,15"
Click="backButton_Click">
                Back
            </Button>
        </Grid>

    </Grid>
</Page>
