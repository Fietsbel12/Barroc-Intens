<Page
    x:Class="BarrocIntens.View.SalesHomepage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#FFFDFDFD">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0" Background="#FF1A1A1A" Padding="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="RolTextBlock"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="#FFFFD300"
                       Text="Rol wordt geladen..." />

            <Button x:Name="backButton"
                    Grid.Column="1"
                    Content="Terug"
                    Click="backButton_Click"
                    Padding="12,6"
                    Background="#FFFFD300"
                    Foreground="#22201D"
                    CornerRadius="4"
                    FontWeight="Bold"
                    BorderThickness="0"/>
        </Grid>

        <!-- CONTENT -->
        <ScrollViewer Grid.Row="1" Padding="20">
            <StackPanel Spacing="14">
                <TextBlock Text="Overzicht van alle koffiezetapparaten"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#22201D"
                           TextAlignment="Center"
                           Margin="0,20,0,10"/>

                <TextBlock x:Name="GeenApparatenText"
                           Text="Er zijn nog geen apparaten toegevoegd."
                           FontSize="18"
                           Foreground="Gray"
                           TextAlignment="Center"
                           Visibility="Collapsed"/>

                <!-- 2 kolommen weergave -->
                <ItemsWrapGrid x:Name="WrapPanel" Orientation="Horizontal" MaximumRowsOrColumns="2" />

                <ListView x:Name="ApparatenListView"
                          SelectionMode="None"
                          IsItemClickEnabled="False"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <!-- Toon items in 2 kolommen -->
                            <ItemsWrapGrid Orientation="Horizontal" 
                                           MaximumRowsOrColumns="2" 
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Top"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Koffiezetapparaat"
                                      xmlns:models="using:BarrocIntens.Models">
                            <Border BorderBrush="#FFE0E0E0" 
                                    BorderThickness="1" 
                                    CornerRadius="10" 
                                    Margin="10"
                                    Background="White"
                                    Padding="10"
                                    Width="250">
                                <StackPanel Spacing="8" HorizontalAlignment="Center">
                                    <Image Width="200"
                                           Height="150"
                                           Stretch="UniformToFill"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,6">
                                        <Image.Source>
                                            <BitmapImage UriSource="{x:Bind FotoPad, Mode=OneWay}"/>
                                        </Image.Source>
                                    </Image>

                                    <TextBlock Text="{x:Bind Naam}" 
                                               FontSize="18" 
                                               FontWeight="Bold" 
                                               Foreground="#22201D"
                                               TextAlignment="Center"/>

                                    <TextBlock Text="{x:Bind Merk}" 
                                               FontSize="15" 
                                               Foreground="Gray"
                                               TextAlignment="Center"/>

                                    <TextBlock FontWeight="SemiBold"
                                               Text="Prijs:"
                                               Margin="0,5,0,0"
                                               TextAlignment="Center"/>
                                    <TextBlock FontSize="15" TextAlignment="Center">
                                        <Run Text="€ " />
                                        <Run Text="{x:Bind Prijs, Mode=OneWay}" />
                                    </TextBlock>

                                    <TextBlock FontWeight="SemiBold"
                                               Text="Voorraad:"
                                               Margin="0,5,0,0"
                                               TextAlignment="Center"/>
                                    <TextBlock Text="{x:Bind Voorraad}" 
                                               FontSize="15"
                                               TextAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
